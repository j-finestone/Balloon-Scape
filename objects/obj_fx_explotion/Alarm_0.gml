//This object is created when the last other partical is destroyed, so everything in put in an alarm to make a deley

image_speed = anim_spd_original;

visible = true;

audio_play_sound(snd_fx_explotion, 0, 0)
